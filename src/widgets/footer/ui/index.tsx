import { Typography } from '@/shared';

export const Footer = () => {
  return (
    <footer className="p-4">
      <Typography color="text-white" size="lg" className="text-center">
        Esc - restart test
      </Typography>
    </footer>
  );
};
